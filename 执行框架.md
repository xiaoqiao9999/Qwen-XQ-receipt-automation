# 单据类一键自动化 —— 运行项目执行框架

> 本文描述**当前运行项目**的执行框架：系统现状、主流程每一步怎么跑、数据怎么流转、脚本如何分工、环境依赖什么、日常怎么操作。
> 运行环境：`D:\千问工作区\自动化（单据类）`（代码与数据同目录）
> 版本：2026-09-07 ｜ 重构期历史决策见文末附录，正文不再按重构阶段叙述。

---

## 一、系统概览

一句话：**扫描件进、成品出**的单据流水线——入库单扫描件经 OCR 识别入库，自动生成采购台账、验收单；发票 PDF 经提取匹配，自动生成付款单。

| 输入 | 处理 | 输出 |
|---|---|---|
| 入库单扫描件（jpg/png） | OCR → 数据库匹配 → 归档 | 采购台账（按月分表）、验收单 docx + PDF |
| 发票 PDF | 首页提取供应商/金额 → 用途匹配 | 付款单 xlsx + PDF、付款台账 |
| 申购单 / 事项审批 PDF | 按金额匹配供用途 | 用途回写数据库 |

核心设计原则（脚本强制执行）：
- **数据库是唯一事实源**：台账/验收单/付款单全部从 `60_数据\单据.db` 派生，改数据只改库；
- **严禁编造**：OCR 看不清、用途有冲突、金额对不上，一律打 WARNING 列清单，由人工定夺，脚本不猜；
- **幂等**：同批文件重复跑不产生重复记录。

## 二、当前状态快照（2026-09-07）

| 项 | 状态 |
|---|---|
| 部署 | GitHub `xiaoqiao9999/XQ-richang` v4 已本机化部署，全流程验证通过 |
| 数据 | **已清空待用**：首批 11 单端到端验证后按用户决定移除（回收站可恢复），DB/成品/归档/OCR 缓存均无业务数据 |
| 模板 | 4 个模板已就位 `50_模板` |
| 首次运行 | 直接丢文件 → 双击 `一键生成.bat` 即可；空库/缺目录会自动建 |
| 待验证项 | 发票→付款单链路（步骤7.5/8~14）尚无真实发票样本，首批发票投入时验证 |

## 三、主流程执行框架（一键生成.py，步骤 0~14）

一次运行按序执行以下步骤，**每步前置检查，条件不满足安全跳过，不报错中断**：

| 步骤 | 做什么 | 输入 | 输出/副作用 | 校验与失败行为 |
|---|---|---|---|---|
| **0 自动分拣** | `10_输入` 根目录散放文件归位：文件名关键词→扩展名→PDF 首页内容三层判定 | 散放文件 | 移入对应子目录；判不了进 `10_输入\待确认` | 同名内容相同去重、不同自动加(2)避让；绝不静默丢文件 |
| **1 OCR 识别** | 逐张识别 `10_输入\入库单扫描件\*.{jpg,jpeg,png}`；优先读人工校对缓存 | 扫描件图片 | 识别行文本（内存） | 引擎异常/读图失败→该图记空，WARNING |
| **2 匹配入库** | 提取单号 `CGSH\d{12,}`，与 receipts 表匹配；无单号→自动补录（日期/供应商/明细三缺一即失败）；识别为续页（无单号）→沿用上张单号合并明细 | 识别结果 + DB | 新单据写入 DB；`all_records`/`all_receipts` | **交叉验证**：每行数量×单价=金额、行合计=单合计，不符只 WARNING |
| **3 扫描件归档** | 匹配成功的原件按 `naming.scan_name` 重命名移入 `40_归档\入库单扫描件` | rename_map | 输入目录清空该件 | 移动前判存在；归档命名冲突即避让 |
| **4 采购台账** | 按**每条记录自身日期**归入 `N月` 工作表（修复过跨月错归 bug），宋体写入，表尾合计 | all_records + 模板/已有台账 | `20_输出\台账\采购台账_2026.xls` | 按单号去重跳过已入账；模板缺工作表报 ERROR 跳该月 |
| **4.5 台账自检** | 台账 vs DB 逐单号比行数与金额，整单/部分缺失自动补入 | 台账 xls + DB | 修复后的台账 | 修复后仍不符→WARNING 列明细请人工 |
| **5~6 验收单 docx** | 解包 `验收单模板.docx`，逐单填日期+17 行明细（**口径保持模板原样，不写入库单号/供货单位表头行**） | all_receipts | `20_输出\验收单\验收单_单号_日期_供应商_金额.docx` | 明细>17 行截断（续页已在步骤2合并） |
| **7 验收单转 PDF** | Word COM 导出（先剥除模板修订/保护标记防挂死）；`SKIP_PDF=1` 时跳过、由 `to_pdf.py` 补 | docx | **`30_待打印\*.pdf`** | Word 失败→soffice 回退（本机未装）；均失败→WARNING |
| **7.5 发票提取** | pdfplumber 读 `10_输入\发票\*.pdf` 首页：价税合计（小写）、销售方名称；新 PDF 才入库 | 发票 PDF | invoices 表新增（usage 可为空） | 提取不完整→WARNING 跳过该张，不编造 |
| **8~14 付款单** | 对每张 processed=0 的发票：定用途→按关键词匹配 `付款单数据库.xlsx` 取账户/开户行→OOXML 填模板（大写金额逐位）→xlsx 落 `20_输出\付款单` | invoices + 付款单模板/数据库 | 付款单 xlsx | 申购单与事项审批用途不一致→**熔断**，进结尾清单；未匹配供应商→WARNING 空账户 |
| **14 付款单转 PDF** | Excel COM（win32com）导出，失败回退 soffice | xlsx | **`30_待打印`** | 转后读回 6 字段逐项验证 OK/FAIL |
| **收尾** | 发票 PDF 移入 `20_输出\发票` + 标 processed=1；付款台账按物料关联追加；打印用途冲突清单与用途空值清单 | — | 付款台账_2026.xls | 结尾集中 WARNING，供人工补填 |

## 四、数据流与状态流转

```
扫描件:  10_输入\入库单扫描件 ──OCR+匹配──▶ receipts/receipt_items(单据.db) ──▶ 台账月份页 / 验收单
                                                        │(原件)
                                                        ▼ 步骤3
                                              40_归档\入库单扫描件(规范命名)

发票PDF: 10_输入\发票 ──步骤7.5──▶ invoices(usage, processed=0) ──步骤8~14──▶ 付款单 + processed=1
                                        ▲                                      │
              申购单/事项审批(按金额匹配供用途)                                   ▼
                                                              20_输出\发票(归档副本) / 付款台账
```

关键状态：
- **入库单**：DB 有记录即可被任意批次的扫描件重复触发匹配（幂等）；补录用 `manage_db.py add`；
- **发票**：`invoices.processed` 0→1 单向，置 1 后不再出付款单；用途为空会在每次运行结尾提示（已登记忽略项除外）；
- **OCR 缓存**：以图片内容 MD5 为键，图内容不变缓存有效，重命名不影响。

## 五、目录与脚本体系

### 5.1 十档目录

| 目录 | 角色 | 读写方 |
|---|---|---|
| `10_输入`(+5 子目录+待确认) | 待处理单据入口，散放即分拣 | 人工写、脚本分拣 |
| `20_输出`（台账/验收单/付款单/发票） | 成品与已处理发票副本 | 脚本写 |
| `30_待打印` | 全部打印用 PDF（验收单+付款单） | 脚本写→人工打印 |
| `40_归档` | 已处理原件，**不可再生凭证** | 脚本移动、人工只读 |
| `50_模板` | 4 个业务模板，只读 | 人工维护 |
| `60_数据` | SQLite `单据.db` | 脚本读写 |
| `70_日志` | 运行报告（run_report 链路） | 脚本写 |
| `80_工具` | 辅助脚本 10 个 | 见 5.2 |
| `90_临时` | 分拣/OCR/解包中间件 | 可随时清空 |

### 5.2 脚本分工

**根目录（执行引擎本体，8 个）**：`一键生成.py` 主流程 ｜ `run.py` 解释器自适应入口 ｜ `一键生成.bat` 双击入口 ｜ `paths.py` 路径中枢（BASE 自适应、`DJ_BASE` 可覆盖） ｜ `db.py` 数据访问 ｜ `naming.py` 命名规范 ｜ `init_db.py` 建库（库非空无源拒重建） ｜ `requirements.txt` 依赖清单。

**80_工具（10 个）**：

| 脚本 | 角色 |
|---|---|
| `ocr_local.ps1` | Windows 内置中文 OCR，行式输出，缓存优先（步骤1 调用） |
| `payment_ledger.py` | 付款台账追加（主流程 import） |
| `manage_db.py` | 查库/补录/改用途/统计 |
| `build_ocr_cache.py` / `seed_db.py` | 校对缓存批量写入 / 种子数据入库（幂等） |
| `rebuild_outputs.py` | 从 DB 重建台账+验收单 docx（口径与主脚本同步） |
| `to_pdf.py` | docx→PDF 补转，输出 30_待打印 |
| `run_report.py` | 主流程包裹器，留档 70_日志 |
| `accept_doc.py` / `archive_outputs.py` | 备用渲染变体 / 成品归档工具 |

## 六、运行环境（当前机器实测，2026-09-07）

| 层 | 值 |
|---|---|
| OS | Windows 11 专业版 10.0.26200（依赖系统 OCR，不支持 Win7/8） |
| Python | **3.14.2** `C:\Users\XQ\AppData\Local\Programs\Python\Python314\python.exe`（依赖装在这）；PATH 中另有 3.14.6 内置运行时缺依赖——**统一用 bat/run.py 启动** |
| 依赖 | lxml 6.1.1、xlrd 2.0.2、xlutils 2.0.0、xlwt 1.3.0、pdfplumber 0.11.10、pywin32 312、pandas 3.0.3、openpyxl 3.1.5（+自动随带 pdfminer/Pillow/pypdfium2） |
| 组件 | Word.Application.16 ✅（验收单转PDF） ｜ Excel.Application.16 ✅（付款单转PDF） ｜ OCR zh-Hans-CN ✅ ｜ LibreOffice ❌ 未装（仅回退，不影响） |

## 七、标准操作（SOP）

- **日常跑批**：单据丢 `10_输入` → 双击 `一键生成.bat` → 看结尾 WARNING 清单 → `30_待打印` 打印、`20_输出` 取台账。
- **补录识别失败的单据**：`python 80_工具\manage_db.py add CGSH单号`（对照原图逐行录入）→ 重跑主流程自动出台账/验收单。
- **修正识别结果**：改 `90_临时\ocr_cache\<MD5>.txt`（一行一字段，对照原图，严禁凭记忆改数）或删除该缓存重识别 → `rebuild_outputs.py` 重建成品。
- **PDF 缺失**：关 Word → `python 80_工具\to_pdf.py`。
- **成品与库不符**：`python 80_工具\rebuild_outputs.py`（docx）+ `to_pdf.py`（PDF）。
- **新机器部署**：拷目录 → `pip install -r requirements.txt` → `python -c "import paths; paths.ensure_dirs()"` → `python init_db.py` → 放 4 模板 → 空跑验证各步骤"跳过"无报错。

## 八、风险与注意事项

- **R1 缺单号补录脆弱**：OCR 三要素缺一即失败丢单，靠校对缓存+manage_db 缓解；
- **R2 交叉验证 WARNING 需人工**：脚本提示不篡改，核 `40_归档` 原图；
- **R3 转换依赖桌面组件**：Word/Excel 被占用或 COM 不可用会使 PDF 环节空转（docx/xlsx 不受影响）；
- **R4 双 Python 解释器**：见第六节，入口统一走 bat；
- **R5 同供应商同金额撞名**：付款单命名含发票名+简称+金额，仍可能同名，重名避让机制兜底；
- **R6 发票金额正则**：`（小写）价税合计` 命中不了真实票版式时金额/用途为空，按 WARNING 人工补；
- **R7 改动边界**：本项目脚本与成品格式的**任何新增操作**（超出原仓库既有行为的）须先经用户确认（2026-09-06 明确规则）。

---

## 附录：重构沿革（简表）

| 日期 | 事项 |
|---|---|
| 2026-09-02 | 原机 v4 重构定稿（TRAE 协助）：JSON→SQLite、发票用途出库、中文前缀命名、10 档目录、旧文件迁移完成；历史 JSON 保留原机未随仓库发布 |
| 2026-09-05 | 本机部署：OCR 本机化（内置引擎+校对缓存）、路径自适应、依赖装齐、空库+骨架建立 |
| 2026-09-06 | 首批 11 单实跑与修复：月份归类 bug、归档命名错位、补录漏网单号、验收单表头行回退（确立 R7 规则）、PDF 口径统一 30_待打印、自动分拣上线、工作区清理 |
| 2026-09-07 | **历史数据清除**：66 文件入回收站（DB 11 单/成品/归档/缓存），保留纯执行引擎；本文档改为运行项目视角 |

历史决策定格：D2 无关脚本不接入（且已从本机清除）｜ D4 定时任务=手动 ｜ D5 历史 JSON 不导入 ｜ D7 历史业务数据清除、只留任务执行部分。

---

生成方：TRAE 协助（原始重构版，2026-09-02）；千问工作助理改写为运行项目执行框架（2026-09-07）。
